<section class="vh-50 bg-image mt-3 mb-5">
    <div class="mask d-flex align-items-center h-100 gradient-custom-3 mt-3">
        <div class="container h-100">
            <div class="row d-flex justify-content-center align-items-center h-100">
                <div class="col-12 col-md-9 col-lg-7 col-xl-6">
                    <div class="card" style="border-radius: 15px;">
                        <div class="card-body p-1">
                            <form action="" method="post" enctype="multipart/form-data" class=" border border-light p-5">
                                <p class="h4 mb-4 text-center">Sửa dịch vụ</p>
                                <!-- NameService -->
                                <input type="text" id="nameService" class="form-control mb-4"
                                       placeholder="Tên dịch vụ" name="nameService"
                                       value="{{itemService.nameService}}">
                                <!-- PriceService -->
                                <input type="text" id="price" class="form-control mb-4"
                                       placeholder="Giá dịch vụ" name="price"
                                       value="{{itemService.price}}">
                                <!-- TimeService -->
                                <input type="text" id="workTime" class="form-control mb-4"
                                       placeholder="Thời gian" name="workTime"
                                       value="{{itemService.workTime}}">
                                <div class="form-group mb-4">
                                    <textarea name="describe" class="form-control rounded-0"
                                              id="exampleFormControlTextarea2" rows="3"
                                              placeholder="Mô tả">{{itemService.describe}}</textarea>
                                </div>
                                <div class="form-outline mb-4">
                                    <label class="form-label" for="form3Example4cdg">Ảnh</label>
                                    <input type="file" name="images" value="{{itemService.images}}" >
<!--                                    <input type="submit" value="uploading_img">-->
                                </div>


                                <div class="d-flex justify-content-center mt-3">
                                    <button type="submit" class="btn btn-warning"
                                            style="background-color: #F9E323;font-weight: bold">Hoàn tất
                                    </button>
                                </div>
                                </p>
                            </form>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>
